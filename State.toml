# DustViz - DPL Visualization Workspace Manifest

[workspace]
name = "dustviz"
version = "0.2.0"
description = "Dust-native IR and constraint graph visualization pipeline"

[sector]
name = "dustviz"
path = "src"

[target.x64-pc-none]
linker = "ld"
linker_flags = ["-nostdlib", "-static"]
